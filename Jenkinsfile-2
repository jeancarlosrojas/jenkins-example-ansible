pipeline {
  agent { 
    label 'slave-lnx-01' 
  }
  environment {}
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-galaxy collection install -r requirements.yml'
      }
    }
  }
}
